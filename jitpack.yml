before_install:
  - sdk install java 21-tem
  - sdk use java 21-tem
installx:
  - ./mvnw clean install -DskipTests -Dquarkus.package.jar.type=uber-jar
env:
  MAVEN_OPTS: "-Dquarkus.package.jar.type=uber-jar"
